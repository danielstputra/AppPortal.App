# Backend — API Services

Backend services are managed by an external vendor.

## Architecture

This directory contains API service configurations for the portal backend.
For API documentation, refer to the vendor's Swagger endpoint.

## Development

API Base URL configuration is in `Frontend/src/Web/appsettings.json`.

```
{VendorApiBaseUrl}/swagger — API documentation
```
