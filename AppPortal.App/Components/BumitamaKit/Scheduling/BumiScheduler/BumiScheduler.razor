@namespace AppPortal.App.Components.BumitamaKit.Scheduling.BumiScheduler

@using DevExpress.Blazor

<div class="bumi-scheduler-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxScheduler CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxScheduler>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-scheduler bumi-scheduler-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
