@namespace AppPortal.App.Components.BumitamaKit.Navigation.BumiAccordion

@using DevExpress.Blazor

<div class="bumi-accordion-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxAccordion CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxAccordion>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-accordion bumi-accordion-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
