@namespace AppPortal.App.Components.BumitamaKit.Navigation.BumiPager

@using DevExpress.Blazor

<div class="bumi-pager-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxPager CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxPager>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-pager bumi-pager-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
