@namespace AppPortal.App.Components.BumitamaKit.Navigation.BumiToolbar

@using DevExpress.Blazor

<div class="bumi-toolbar-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxToolbar CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxToolbar>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-toolbar bumi-toolbar-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
