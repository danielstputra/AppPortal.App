@namespace AppPortal.App.Components.BumitamaKit.Navigation.BumiMenu

@using DevExpress.Blazor

<div class="bumi-menu-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxMenu CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxMenu>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-menu bumi-menu-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
