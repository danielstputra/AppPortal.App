@namespace AppPortal.App.Components.BumitamaKit.Navigation.BumiTreeView

@using DevExpress.Blazor

<div class="bumi-treeview-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxTreeView CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxTreeView>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-treeview bumi-treeview-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
