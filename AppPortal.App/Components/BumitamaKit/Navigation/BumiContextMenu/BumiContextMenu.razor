@namespace AppPortal.App.Components.BumitamaKit.Navigation.BumiContextMenu

@using DevExpress.Blazor

<div class="bumi-contextmenu-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxContextMenu CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxContextMenu>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-contextmenu bumi-contextmenu-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
