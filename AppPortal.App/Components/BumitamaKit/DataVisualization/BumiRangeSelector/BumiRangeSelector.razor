@namespace AppPortal.App.Components.BumitamaKit.DataVisualization.BumiRangeSelector

@using DevExpress.Blazor

<div class="bumi-rangeselector-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxRangeSelector CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxRangeSelector>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-rangeselector bumi-rangeselector-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
