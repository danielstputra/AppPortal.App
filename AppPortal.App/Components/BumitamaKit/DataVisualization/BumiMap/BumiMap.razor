@namespace AppPortal.App.Components.BumitamaKit.DataVisualization.BumiMap

@using DevExpress.Blazor

<div class="bumi-map-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxMap CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxMap>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-map bumi-map-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
