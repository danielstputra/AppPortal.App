@namespace AppPortal.App.Components.BumitamaKit.DataVisualization.BumiBarGauge

@using DevExpress.Blazor

<div class="bumi-bargauge-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxBarGauge CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxBarGauge>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-bargauge bumi-bargauge-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
