@namespace AppPortal.App.Components.BumitamaKit.Layout.BumiDrawer

@using DevExpress.Blazor

<div class="bumi-drawer-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxDrawer CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxDrawer>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-drawer bumi-drawer-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
