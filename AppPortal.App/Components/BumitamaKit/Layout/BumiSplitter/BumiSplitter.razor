@namespace AppPortal.App.Components.BumitamaKit.Layout.BumiSplitter

@using DevExpress.Blazor

<div class="bumi-splitter-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxSplitter CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxSplitter>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-splitter bumi-splitter-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
