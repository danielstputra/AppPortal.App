@namespace AppPortal.App.Components.BumitamaKit.Layout.BumiCarousel

@using DevExpress.Blazor

<div class="bumi-carousel-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxCarousel CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxCarousel>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-carousel bumi-carousel-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
