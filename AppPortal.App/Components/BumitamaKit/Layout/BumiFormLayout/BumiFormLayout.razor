@namespace AppPortal.App.Components.BumitamaKit.Layout.BumiFormLayout

@using DevExpress.Blazor

<div class="bumi-formlayout-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxFormLayout CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxFormLayout>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-formlayout bumi-formlayout-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
