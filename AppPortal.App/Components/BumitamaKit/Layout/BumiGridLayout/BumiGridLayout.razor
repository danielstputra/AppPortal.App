@namespace AppPortal.App.Components.BumitamaKit.Layout.BumiGridLayout

@using DevExpress.Blazor

<div class="bumi-gridlayout-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxGridLayout CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxGridLayout>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-gridlayout bumi-gridlayout-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
