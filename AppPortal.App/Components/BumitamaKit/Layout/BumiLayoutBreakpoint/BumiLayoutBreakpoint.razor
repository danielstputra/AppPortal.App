@namespace AppPortal.App.Components.BumitamaKit.Layout.BumiLayoutBreakpoint

@using DevExpress.Blazor

<div class="bumi-layoutbreakpoint-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxLayoutBreakpoint CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxLayoutBreakpoint>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-layoutbreakpoint bumi-layoutbreakpoint-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
