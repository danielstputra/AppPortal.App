@namespace AppPortal.App.Components.BumitamaKit.Reporting.BumiReport

@using DevExpress.Blazor

<div class="bumi-report-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxReportViewer CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxReportViewer>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-report bumi-report-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
