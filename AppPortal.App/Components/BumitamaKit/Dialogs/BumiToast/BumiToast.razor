@namespace AppPortal.App.Components.BumitamaKit.Dialogs.BumiToast

@using DevExpress.Blazor

<div class="bumi-toast-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxToast CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxToast>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-toast bumi-toast-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
