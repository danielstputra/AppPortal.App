@namespace AppPortal.App.Components.BumitamaKit.Dialogs.BumiPopup

@using DevExpress.Blazor

<div class="bumi-popup-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxPopup CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxPopup>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-popup bumi-popup-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
