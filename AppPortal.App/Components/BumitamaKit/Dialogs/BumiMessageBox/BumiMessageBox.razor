@namespace AppPortal.App.Components.BumitamaKit.Dialogs.BumiMessageBox

@using DevExpress.Blazor

<div class="bumi-messagebox-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxMessageBox CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxMessageBox>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-messagebox bumi-messagebox-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
