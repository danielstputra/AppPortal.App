@namespace AppPortal.App.Components.BumitamaKit.Dialogs.BumiDropDown

@using DevExpress.Blazor

<div class="bumi-dropdown-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxDropDown CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxDropDown>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-dropdown bumi-dropdown-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
