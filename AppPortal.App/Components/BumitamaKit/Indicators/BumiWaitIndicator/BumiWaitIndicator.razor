@namespace AppPortal.App.Components.BumitamaKit.Indicators.BumiWaitIndicator

@using DevExpress.Blazor

<div class="bumi-waitindicator-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxLoadingPanel CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxLoadingPanel>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-waitindicator bumi-waitindicator-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
