@namespace AppPortal.App.Components.BumitamaKit.FileManagement.BumiFileInput

@using DevExpress.Blazor

<div class="bumi-fileinput-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxUpload CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxUpload>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-fileinput bumi-fileinput-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
