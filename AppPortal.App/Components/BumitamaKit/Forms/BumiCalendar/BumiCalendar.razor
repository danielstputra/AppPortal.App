@namespace AppPortal.App.Components.BumitamaKit.Forms.BumiCalendar

@using DevExpress.Blazor

<div class="bumi-calendar-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxCalendar T="DateTime?" CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxCalendar>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-calendar bumi-calendar-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}

