@namespace AppPortal.App.Components.BumitamaKit.Forms.BumiListBox

@using DevExpress.Blazor

<div class="bumi-listbox-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxListBox TData="string" TValue="string" CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxListBox>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-listbox bumi-listbox-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}

