@namespace AppPortal.App.Components.BumitamaKit.Forms.BumiSearchBox

@using DevExpress.Blazor

<div class="bumi-searchbox-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxTextBox CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxTextBox>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-searchbox bumi-searchbox-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
