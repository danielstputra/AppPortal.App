@namespace AppPortal.App.Components.BumitamaKit.Forms.BumiTimeEdit

@using DevExpress.Blazor

<div class="bumi-timeedit-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxTimeEdit T="TimeSpan?" CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxTimeEdit>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-timeedit bumi-timeedit-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}

