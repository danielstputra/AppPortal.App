@namespace AppPortal.App.Components.BumitamaKit.Forms.BumiRadio

@using DevExpress.Blazor

<div class="bumi-radio-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxRadioButton CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxRadioButton>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-radio bumi-radio-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
