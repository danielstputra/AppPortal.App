@namespace AppPortal.App.Components.BumitamaKit.DataManagement.BumiTreeList

@using DevExpress.Blazor

<div class="bumi-treelist-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxTreeList CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxTreeList>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-treelist bumi-treelist-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
