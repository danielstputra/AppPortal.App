@namespace AppPortal.App.Components.BumitamaKit.DataManagement.BumiFilterBuilder

@using DevExpress.Blazor

<div class="bumi-filterbuilder-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxFilterBuilder CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxFilterBuilder>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-filterbuilder bumi-filterbuilder-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
