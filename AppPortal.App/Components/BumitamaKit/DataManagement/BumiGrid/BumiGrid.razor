@namespace AppPortal.App.Components.BumitamaKit.DataManagement.BumiGrid

@using DevExpress.Blazor

<div class="bumi-grid-theme bumi-theme-@Theme.ToString().ToLower()" style="@GetWrapperStyle()">
    <DxGrid CssClass="@GetDynamicCssClass()" style="@GetComponentStyle()">
        @ChildContent
    </DxGrid>
</div>

@code {
    private string GetWrapperStyle()
    {
        if (!string.IsNullOrEmpty(CustomColor))
        {
            return $"--bumi-custom-color: {CustomColor};";
        }
        return "";
    }

    private string GetComponentStyle()
    {
        var styles = new List<string>();
        return string.Join("; ", styles);
    }

    private string GetDynamicCssClass()
    {
        var classes = $"bumi-grid bumi-grid-{Theme.ToString().ToLower()}";
        if (!string.IsNullOrEmpty(CssClass))
        {
            classes += $" {CssClass}";
        }
        return classes.Trim();
    }
}
